{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch hello",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/hello-project/hello.go"
        }
    ]
}